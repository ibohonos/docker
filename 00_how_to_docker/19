docker run -it --name Abathur -v $HOME:/root -p 3000:3000 python:2-slim bash

# apt-get update
# apt-get install -y python-pip python-dev build-essential nano
# pip install flask

# nano ~/app.py (to paste hello world info from http://flask.pocoo.org/)
# export FLASK_APP=/root/app.py
# flask run -h 0.0.0.0 -p 3000
